# 霸王搞姬 👑 成都中高端外围工作室

一个基于 Vue 3 + Vite 的前端展示网站，展示妹子图片、价目表、预约流程等信息。

## 技术栈

- **前端框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **样式**: CSS (scoped)
- **部署**: 静态站点，可部署到 Vercel、Netlify、GitHub Pages 等

## 安装

确保你已安装 Node.js (推荐版本 16+)

```bash
# 克隆项目
git clone <repository-url>
cd vue-front

# 安装依赖
npm install
```

## 运行

```bash
# 启动开发服务器
npm run dev
```

访问 http://localhost:5173 查看页面

## 构建

```bash
# 构建生产版本
npm run build
```

构建后的文件在 `dist` 文件夹中

## 部署

### 方式一：Vercel (推荐)

1. 注册 Vercel 账号并连接 GitHub
2. 导入项目仓库
3. Vercel 会自动检测为 Vite 项目并配置
4. 部署完成，获得访问链接

### 方式二：Netlify

1. 注册 Netlify 账号
2. 拖拽 `dist` 文件夹到 Netlify 界面，或连接 GitHub 仓库
3. 设置构建命令为 `npm run build`，发布目录为 `dist`
4. 部署完成

### 方式三：GitHub Pages

1. 构建项目：`npm run build`
2. 安装 gh-pages：`npm install -g gh-pages`
3. 部署：`gh-pages -d dist`
4. 在 GitHub 仓库设置中启用 Pages，选择 gh-pages 分支

### 方式四：其他静态托管

将 `dist` 文件夹上传到任何支持静态文件的服务器或 CDN

## 项目结构

```
vue-front/
├── public/          # 静态资源 (图片等)
│   └── mm/         # 妹子图片文件夹
├── src/
│   ├── assets/     # 资源文件
│   ├── components/ # Vue 组件
│   ├── App.vue     # 主应用组件
│   ├── main.js     # 应用入口
│   └── mmImages.js # 图片路径配置
├── index.html      # HTML 模板
├── package.json    # 项目配置
└── vite.config.js  # Vite 配置
```

## 注意事项

- 图片资源需放置在 `public/mm/` 目录下
- 确保图片路径在 `src/mmImages.js` 中正确配置
- 生产部署时注意图片加载路径

## 许可证

MIT License
